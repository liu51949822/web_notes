{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // Path mapping for components directory
      "@/components/*": ["components/*"],
      // Path mapping for lib directory
      "@/lib/*": ["lib/*"]
    }
  }
}